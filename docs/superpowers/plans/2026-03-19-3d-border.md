# 3D Border Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer toutes les bordures SVG statiques par une bordure 3D R3F (fixed overlay) qui s'incline au scroll.

**Architecture:** Un composant `Border3D` monte un Canvas R3F en `position: fixed` avec caméra orthographique. La géométrie est un `THREE.Shape` rectangulaire plein blanc avec un trou intérieur arrondi, reconstruite au resize. GSAP ScrollTrigger pilote `rotateX` de -8° à +12° sur toute la page.

**Tech Stack:** React 19, Three.js ^0.168, @react-three/fiber ^9, @react-three/drei ^10, GSAP ScrollTrigger

---

## File Map

| Action | Fichier | Rôle |
|--------|---------|------|
| Create | `src/components/Border3D/index.jsx` | Canvas R3F + mesh + scroll animation |
| Modify | `src/components/Intro/index.jsx` | Retirer Border, ajouter Menu directement |
| Modify | `src/components/App/App.jsx` | Retirer SVGs/refs/effects de bordure, ajouter Border3D |
| Delete | `src/components/Border/` | Ancienne bordure SVG — plus utilisée |

---

### Task 1 : Installer les dépendances

**Files:**
- Modify: `package.json`

- [ ] **Step 1 : Installer**

```bash
cd /Users/theo/Documents/Dev/Artic-Monkeys
npm install three@^0.168.0 @react-three/drei@^10.0.0
```

- [ ] **Step 2 : Vérifier**

```bash
cat node_modules/three/package.json | grep '"version"'
```

Expected : `"version": "0.168.x"` ou supérieure.

- [ ] **Step 3 : Commit**

```bash
git add package.json package-lock.json
git commit -m "deps: bump three to 0.168, add drei"
```

---

### Task 2 : Créer le composant Border3D

**Files:**
- Create: `src/components/Border3D/index.jsx`

- [ ] **Step 1 : Créer le fichier**

```jsx
// src/components/Border3D/index.jsx
import { useEffect, useRef, useState } from 'react'
import { Canvas, useThree } from '@react-three/fiber'
import * as THREE from 'three'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

function buildBorderShape(w, h, thickness, radius) {
  const ow = w / 2, oh = h / 2
  const iw = ow - thickness, ih = oh - thickness
  const r = Math.min(radius, iw, ih)

  const shape = new THREE.Shape()
  shape.moveTo(-ow, -oh)
  shape.lineTo( ow, -oh)
  shape.lineTo( ow,  oh)
  shape.lineTo(-ow,  oh)
  shape.closePath()

  const hole = new THREE.Path()
  hole.moveTo(-iw + r, -ih)
  hole.lineTo( iw - r, -ih)
  hole.absarc( iw - r, -ih + r, r, -Math.PI / 2, 0, false)
  hole.lineTo( iw,  ih - r)
  hole.absarc( iw - r,  ih - r, r, 0, Math.PI / 2, false)
  hole.lineTo(-iw + r,  ih)
  hole.absarc(-iw + r,  ih - r, r, Math.PI / 2, Math.PI, false)
  hole.lineTo(-iw, -ih + r)
  hole.absarc(-iw + r, -ih + r, r, Math.PI, Math.PI * 1.5, false)
  hole.closePath()

  shape.holes.push(hole)
  return new THREE.ShapeGeometry(shape)
}

function BorderMesh() {
  const { size } = useThree()
  const meshRef = useRef(null)
  const [geometry, setGeometry] = useState(null)

  // Rebuild geometry on resize, dispose old one
  useEffect(() => {
    const geo = buildBorderShape(size.width, size.height, 20, 36)
    setGeometry(geo)
    return () => geo.dispose()
  }, [size.width, size.height])

  // Scroll animation — runs once on mount
  useEffect(() => {
    const mesh = meshRef.current
    if (!mesh) return

    mesh.rotation.x = THREE.MathUtils.degToRad(-8)
    mesh.rotation.y = THREE.MathUtils.degToRad(-3)

    const trigger = ScrollTrigger.create({
      trigger: document.body,
      start: 'top top',
      end: 'bottom bottom',
      scrub: 1,
      onUpdate: (self) => {
        mesh.rotation.x = THREE.MathUtils.degToRad(-8 + self.progress * 20)
      },
    })

    return () => trigger.kill()
  }, [])

  if (!geometry) return null

  return (
    <mesh ref={meshRef}>
      <primitive object={geometry} attach="geometry" />
      <meshBasicMaterial color="#ffffff" side={THREE.DoubleSide} />
    </mesh>
  )
}

export default function Border3D() {
  return (
    <Canvas
      orthographic
      camera={{ zoom: 1, near: 0.1, far: 1000, position: [0, 0, 100] }}
      style={{
        position: 'fixed',
        inset: 0,
        zIndex: 9999,
        pointerEvents: 'none',
      }}
    >
      <BorderMesh />
    </Canvas>
  )
}
```

- [ ] **Step 2 : Commit**

```bash
git add src/components/Border3D/index.jsx
git commit -m "feat: add Border3D R3F component"
```

---

### Task 3 : Brancher Border3D dans App.jsx

**Files:**
- Modify: `src/components/App/App.jsx`

- [ ] **Step 1 : Ajouter l'import en haut du fichier**

Ajouter après les imports existants :
```jsx
import Border3D from '../Border3D'
```

- [ ] **Step 2 : Ajouter Border3D comme premier enfant du return**

Dans le `return`, avant le `<div>` du grain overlay (ligne ~138) :
```jsx
return (
  <>
    <Border3D />
    <CustomCursor />
    <div style={{ position: "fixed", inset: 0, zIndex: 9998, pointerEvents: "none" }}>
      {/* grain overlay — inchangé */}
    </div>
    ...
```

- [ ] **Step 3 : Vérifier visuellement**

```bash
npm run dev
```

Ouvrir le navigateur. La bordure blanche doit apparaître autour du viewport, légèrement inclinée. Le site doit rester visible à l'intérieur. En scrollant, la bordure doit s'incliner progressivement.

- [ ] **Step 4 : Commit**

```bash
git add src/components/App/App.jsx
git commit -m "feat: mount Border3D in App"
```

---

### Task 4 : Supprimer les SVGs de bordure dans App.jsx

**Files:**
- Modify: `src/components/App/App.jsx`

- [ ] **Step 1 : Retirer les refs de bordure**

Supprimer ces 5 lignes dans les déclarations de refs (vers la ligne 25) :
```jsx
const borderTopRef = useRef(null)
const borderTopRightRef = useRef(null)
const borderBottomLeftRef = useRef(null)
const borderBottomRightRef = useRef(null)
const borderHexRef = useRef(null)
```

- [ ] **Step 2 : Supprimer le useEffect de scroll/parallax sur les bordures**

Supprimer le bloc `useEffect` complet qui cible `sectionRef` et anime les 5 refs de bordure (lignes ~47–93, le second useEffect du composant). Il commence par `if (!contentVisible) return` et contient les `gsap.set` et `gsap.to` sur les 5 refs.

**Ne pas supprimer** le premier `useEffect` qui cible `.bg-white.relative` et anime `svgCentralRef` (lignes ~32–45) — il est sans rapport avec les bordures.

- [ ] **Step 3 : Supprimer la section wrapper et ses SVGs**

Supprimer le bloc `<section ref={sectionRef} ...>` entier (lignes ~129–164 dans la version originale). Ce bloc contient :
- Le SVG `borderTopRef` (trapèze)
- Le SVG `borderTopRightRef` (diamants)
- Le `div.mt-7` avec `<Group />` et les SVGs `borderBottomLeftRef`, `borderBottomRightRef`
- Le SVG `borderHexRef` (étoile)

**Remplacer ce bloc par juste `<Group svgCentralRef={svgCentralRef} />`** (Group est rendu à l'intérieur et doit être conservé) :

```jsx
{/* Remplace toute la <section ref={sectionRef}> */}
<Group svgCentralRef={svgCentralRef} />
```

- [ ] **Step 4 : Supprimer sectionRef**

Supprimer la déclaration `const sectionRef = useRef(null)` (ligne ~30).

- [ ] **Step 5 : Vérifier visuellement**

```bash
npm run dev
```

Le site doit s'afficher normalement, sans les anciennes bordures SVG. Seule la bordure R3F doit être visible.

- [ ] **Step 6 : Commit**

```bash
git add src/components/App/App.jsx
git commit -m "refactor: remove legacy SVG borders from App"
```

---

### Task 5 : Migrer Menu dans Intro, supprimer Border

**Files:**
- Modify: `src/components/Intro/index.jsx`
- Delete: `src/components/Border/` (dossier entier)

- [ ] **Step 1 : Modifier Intro/index.jsx**

3 changements ciblés dans `src/components/Intro/index.jsx` :

**1. Remplacer l'import Border par Menu :**
```jsx
// Supprimer :
import Border from "../Border";
// Ajouter :
import Menu from "../Menu";
```

**2. Retirer l'import `useState` s'il n'est pas utilisé ailleurs dans le fichier** (vérifier d'abord).

**3. Remplacer `<Border />` par `<Menu />` dans le JSX :**
```jsx
// Remplacer :
<div className="absolute top-0 left-0">
  <Border />
</div>
// Par :
<div className="absolute top-0 left-0">
  <Menu />
</div>
```

- [ ] **Step 2 : Supprimer le dossier Border**

```bash
rm -rf /Users/theo/Documents/Dev/Artic-Monkeys/src/components/Border
```

- [ ] **Step 3 : Vérifier visuellement**

```bash
npm run dev
```

Le menu doit toujours fonctionner (bouton "Menu" visible et cliquable). Aucune erreur console liée à Border.

- [ ] **Step 4 : Commit**

```bash
git add src/components/Intro/index.jsx
git rm -r src/components/Border/
git commit -m "refactor: migrate Menu to Intro, delete Border component"
```

---

### Task 6 : Vérification finale

- [ ] **Step 1 : Vérifier le build**

```bash
npm run build
```

Expected : build sans erreur.

- [ ] **Step 2 : Checklist visuelle**

- [ ] Bordure blanche visible autour du viewport
- [ ] Coins extérieurs du viewport couverts (blanc, pas de noir aux angles)
- [ ] Trou intérieur arrondi, site visible dedans
- [ ] Légère inclinaison au repos (-8° rotateX)
- [ ] Inclinaison progressive au scroll (vers +12°)
- [ ] Menu toujours fonctionnel
- [ ] Ancien grain overlay toujours visible (z-index 9998)
- [ ] Aucune console error

- [ ] **Step 3 : Commit final si des fichiers restent non commités**

```bash
git status
git add src/components/Border3D/index.jsx src/components/App/App.jsx src/components/Intro/index.jsx
git commit -m "feat: 3D border complete"
```
